const {
  SITE_NAME,
  OGP_WORKER,
  OG_IMAGE_URL,
  DESCRIPTION,
  KEYWORDS,
  ANALYTICS_TOKEN,
  THEME,
} = process.env

export {
  SITE_NAME,
  OGP_WORKER,
  OG_IMAGE_URL,
  DESCRIPTION,
  KEYWORDS,
  ANALYTICS_TOKEN,
  THEME,
}
